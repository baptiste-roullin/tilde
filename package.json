{
  "name": "prout",
  "version": "0.2.0",
  "engines": {
    "node": "20.0.0"
  },
  "scripts": {
    "dev": "eleventy --incremental --serve",
    "build": "eleventy  --incremental",
    "deploy": "bash ./deploy.sh"
  },
  "devDependencies": {
    "@11ty/eleventy": "v2.0.1"
  }
}